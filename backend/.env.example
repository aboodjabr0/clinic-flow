# Copy this file's values into real environment variables before running
# `dotnet run` (e.g. `export $(grep -v '^#' .env.local | xargs)` or set them
# in your shell/IDE run configuration). ASP.NET Core does not load .env
# files automatically.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=clinicflow_dev
DB_USER=clinicflow_dev
DB_PASSWORD=change-me

ASPNETCORE_ENVIRONMENT=Development
